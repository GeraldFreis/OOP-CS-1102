#include <iostream>
#include <iostream>
using namespace std;

void cpyia(int old_array[], int new_array[], int length){
    // creating the pointers
    int *oldptr, *newptr;
    oldptr = &old_array[0];
    newptr = &new_array[0];

    

}